/layer*.md

node_modules/
dist/
build/
vscode-extension/bin/
vscode-extension/*.vsix
/.claude/
/.mcp.json
/.vtrace/
/.mytool/
/.vtrace-test-tmp/
/.codex
/capsule

# Stage 5B reproduces SWE-bench task checkouts by cloning upstream repos here.
benchmarks/stage5_vexp_swe_bench_smoke/results/workspaces/
# The live Capsule v2 precheck builds its own isolated clones + context here.
benchmarks/stage5_vexp_swe_bench_smoke/results/precheck/

# Raw agent stream-json captured for pivot-inspection tool-call telemetry. It is
# a large raw log; the normalized `_tool_calls.json` is derived from it. Not
# tracked by default (the per-run raw artifacts are untracked too).
_agent_stream.jsonl
